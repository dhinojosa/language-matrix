/**
 * The assert keyword
 * Groovy asserts are an easy way to verify correctness
 * An assert returns nothing if the expression is true
 * and prints a detailed error message if it’s not.
 */

assert(true)
assert(3 == 3)
assert('a' == 'b') //fails

